import java.util.*;
    public class linkedlist {
        public static void main(String[] args){
            Scanner sc=new Scanner(System.in);
            int l1[100],l2[100],i,sum,n;
            l1=sc.nextInt();
            l2=sc.nextInt();
            for(i=0;i<n,i++)
            {
                int rev=0;
               // while(n!=0){
                    rev = rev * 10 + (n % 10);
                    n=n/10;
              //}
            }
            System.out.println(rev);
            for(i=0;i<n,i++)
            {
                int rev1=0;
               // while(n!=0){
                    rev1 = rev 1* 10 + n%10;
                    n=n/10;
              //}
            }
            System.out.println(rev1);
            sum = rev+rev1;
            System.out.println(sum);

        }
    }
